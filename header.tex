

% Load the listings package for code formatting
\usepackage{listings}
% Load xcolor for enhanced color options
\usepackage{xcolor}

% Set the appearance of code chunks globally
\lstset{
   % basicstyle=\ttfamily\scriptsize, % Use scriptsize to make the font smaller
  breaklines=true, % Allow lines to break
  frame=single, % Add a frame around the code block
  keywordstyle=\color{blue}, % Optional: Add color to keywords
  commentstyle=\color{gray}, % Optional: Add color to comments
  stringstyle=\color{red} % Optional: Add color to strings
}

